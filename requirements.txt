# GAL — Python Dependencies
# Install: pip install -r requirements.txt

# Core web framework
flask>=3.1.0
Werkzeug>=3.1.0

# Environment variables
python-dotenv>=1.0.0

# Database (optional — falls back to local JSON if not installed)
pymongo>=4.10.0

# Firebase (optional)
# firebase-admin>=6.0.0

# Production server for Render deployment
gunicorn>=23.0.0
